class Solution {
public:
    vector<int> runningSum(vector<int>& nums) {
        vector<int>runningSum(nums.size());
        int som=0;
        for(int i=0;i<nums.size();i++){
            som+=nums[i];
            runningSum[i]=som;
        }
    return  runningSum;
    }
};
