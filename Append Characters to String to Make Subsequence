class Solution {
public:
    int appendCharacters(string s, string t) {
        int val=0;
        for(int i=0;i<s.length();i++){
            if(val==t.length())break;
            if(s[i]==t[val]){
                val++;
            }
        }
    return t.length()-val;
    }
};
