class Solution {
public:
    bool checkIfPangram(string sentence) {
        map<char,bool>vis;
        for(int i=0;i<sentence.size();i++){
            vis[sentence[i]]=1;
        }
        return(vis.size()==26);

    }
};
